# Makefile generated by imake - do not edit!
# $Xorg: imake.c,v 1.6 2001/02/09 02:03:15 xorgcvs Exp $

# $Id: Makefile,v 1.6 2010/11/17 17:34:58 deraugla Exp $

# $Id: Makefile,v 1.6 2010/11/17 17:34:58 deraugla Exp $

TOP=../../.
CURRENT_DIR=./examples/abalone
AR=ar
ARFLAGS=qc
OCAMLC=ocamlc.opt
OCAMLOPT=ocamlopt.opt
CAMLLINK=ocamlc.opt
CAMLLIBR=ocamlc.opt -a
CAMLOPTLIBR=ocamlopt -a
CAMLMKTOP=ocamlmktop
CAMLP5=camlp5r
CC=gcc
ZDEP=ocamldep -pp $(CAMLP5)
RTDIR=$(TOP)/rt
RTLIB=$(RTDIR)/librt.cma
XLIB=$(TOP)/Xlib/$(ARCH)/libx.a
CONFIG=$(TOP)/config

ARCH=.

BINDIR=/usr/local/bin
LIBDIR=/usr/local/lib/ocaml
LIBX11=-cclib -L/usr/X11R6/lib -cclib -lX11
OLIBDIR=/usr/local/lib/ocaml

ZDEPFLAGS=-I $(RTDIR)
ZFLAGS=-I $(RTDIR) -I `camlp5 -where`
CFLAGS=-O -I$(OLIBDIR) -I/usr/X11R6/include

all::

# (begin Imakefile)

# $Id: Makefile,v 1.6 2010/11/17 17:34:58 deraugla Exp $

TARGET=$(ARCH)/abalone.out
ZOFILES=common.cmo abalone.cmo computer.cmo xabalone.cmo go.cmo
XLIB=$(TOP)/Xlib/$(ARCH)/libx.a

all:: $(TARGET)

$(TARGET): $(RTLIB) $(XLIB)  $(ZOFILES)
	$(CAMLLINK) -custom $(RTLIB)   $(ZOFILES) $(XLIB) $(LIBX11) \
 -o $@

clean::
	rm -f $(TARGET)

depend::
	$(ZDEP) $(ZDEPFLAGS) $$(ls *.ml *.mli | LC_ALL=C sort) >> Imakefile
zofiles:: $(ZOFILES)

# ## DO NOT DELETE THIS LINE
abalone.cmo: common.cmo
abalone.cmx: common.cmx
computer.cmo: common.cmo computer.cmi
computer.cmx: common.cmx computer.cmi
computer.cmi: common.cmo
go.cmo: xabalone.cmo abalone.cmo
go.cmx: xabalone.cmx abalone.cmx
xabalone.cmo: ../.././rt/rt.cmi computer.cmi common.cmo
xabalone.cmx: ../.././rt/rt.cmx computer.cmx common.cmx

# (end Imakefile)

clean scratch::
	rm -f *.run *.out *.cm[iox] *.o *.a *.bak *~ *.log

install::

rminstall::

depend:: Makefile

save::
	-ci -q -m"- sauvegarde" RCS/*,v
restore::
	-co -q RCS/*,v

rcs_release::
	-rcs -q -sRel RCS/*,v

Makefiles::

deleteMakefiles::

%.cmo: %.ml
	$(OCAMLC) -pp $(CAMLP5) -c $(ZFLAGS) $<

%.cmx: %.ml
	$(OCAMLOPT) -pp $(CAMLP5) -c $(ZFLAGS) $<

%.cmi: %.mli
	$(OCAMLC) -pp $(CAMLP5) -c $(ZFLAGS) $<

%.ml: %.mlp
	@/bin/rm -f $@
	cpp -I/usr/X11R6/include -P $< | cat -s > $@
	@chmod -w $@

%.mli: %.mlip
	@/bin/rm -f $@
	cpp -I/usr/X11R6/include -P $< | cat -s > $@
	@chmod -w $@

%.out: %.c
	$(CC) $(CFLAGS) $< -o $@

